{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pytest",
            "type": "shell",
            "command": "${config:python.pythonPath} -m pytest"
        },
        {
            "label": "pip install -e .[test]",
            "type": "shell",
            "command": "${config:python.pythonPath} -m pip install -e .[test]"
        },
        {
            "label": "pip freeze",
            "type": "shell",
            "command": "${config:python.pythonPath} -m pip freeze"
        }
    ]
}
